{
  "name": "codex-mesh-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "dev:lan": "set -a; . ./.env.lan; set +a; npm run dev",
    "build": "npm run build -w web && npm run build -w server",
    "start": "npm run start -w server",
    "check": "npm run check -w server && npm run check -w web",
    "e2e:control": "npm run build && node scripts/control-plane-e2e.mjs",
    "e2e:local": "node scripts/local-e2e.mjs",
    "e2e:real": "node scripts/real-codex-e2e.mjs",
    "e2e:devbox": "npm run build && node scripts/devbox-real-e2e.mjs",
    "e2e:worktree": "npm run build -w server && node scripts/worktree-e2e.mjs",
    "protocol:generate": "codex app-server generate-ts --experimental --out protocol"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
